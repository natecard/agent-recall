# Getting Started

1. Install `agent-recall`.
2. Run `agent-recall init` in a repository.
3. Start a session with `agent-recall start "task"`.
4. Log observations with `agent-recall log`.
5. End the session and compact knowledge with `agent-recall end` then `agent-recall compact`.
