# Style Guide

- Emit loop-derived notifications via shared dispatch helpers for CLI/TUI and hooks  
- Reuse ClaudeCodeIngester parsing to maintain live watcher event consistency with ingestion  
- Store iteration diffs alongside reports and reuse Textual modal/command palette patterns